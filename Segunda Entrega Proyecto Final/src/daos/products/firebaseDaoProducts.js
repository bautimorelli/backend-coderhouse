import { FirebaseContainer } from "../../containers/firebaseContainer.js"

class FirebaseDAOProducts extends FirebaseContainer {
	constructor(nameCollection) {
		super(nameCollection)
	}
}

export { FirebaseDAOProducts }
